# Square supervised labels (E2)

**Status:** placeholder. Push-T uses heuristics in [`../pusht/build_zarr.py`](../pusht/build_zarr.py).

**Planned:** VLM-based labeling → `data/raw/square/square.zarr` with `option_id_supervised`, same zarr layout as Push-T ([`project_plan.md`](../../../../project_plan.md) §5).

**Owner / runbook:** §7 row 29 (Neetish).

When implemented, add `build_zarr.py` (and optional `vlm_label.py`) here; Colab can remain a validation notebook with a link in the script header, matching Push-T.
