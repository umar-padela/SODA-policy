# Frozen Columbia Diffusion Policy — Square (placeholder; mirror pusht/dp_frozen.yaml when wired).

task: square
method: diffusion_policy_cnn_image
train_seed: train_0

checkpoint:
  url: null
  filename: latest.ckpt
  volume_path: null

eval:
  n_action_steps: 1
  smoke:
    n_test: 5
    max_steps: 300
  full:
    n_test: 50
    max_steps: 300
